group 'com.selonj'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.11','org.mybatis:mybatis:3.2.2','mysql:mysql-connector-java:5.1.21'
}

apply plugin: 'idea'

idea {
    module {
        excludeDirs += [file('.idea'), file('gradle'), file('.gradle')]
    }
}